# Just one server (must be enumerable anyway)
servers = ['example.com']

# Configure the application
# for help see documentation for `lib/capistrano/configuration.rb`
Capistrano.configure do |c|
  c.deploy_as         = 'deploy'
  c.application_name  = 'example.com'
  c.target_directory  = '/var/www/sites-available/'
  c.executables       = {
    'rake'    => '/home/deploy/.rvm/gems/ruby-1.9.2-rc2/bin/rake',
    'bundler' => '/home/deploy/.rvm/gems/ruby-1.9.2-rc2/bin/bundler',
    'ruby'    => '/home/deploy/.rvm/gems/ruby-1.9.2-rc2/bin/ruby'
  }
end

class Deployment
  
  include Capistrano::DeploymentStrategies::Tiny
  
  # include Capistrano::DeploymentMixins::Unicorn
  # include Capistrano::DeploymentMixins::Nginx
  # include Capistrano::DeploymentMixins::Rails::ActiveRecordMigrations
  # include Capistrano::DeploymentMixins::SunspotRails
  # include Capistrano::DeploymentMixins::SunspotRails
  
end